<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Public Agent Discussions{% endblock %}</title>
    {# Add links to Bootstrap CSS or your admin theme's core CSS if needed #}
    {# Example using Bootstrap CDN for simplicity: #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {% block head_stylesheets %}{% endblock %}
</head>
<body>
    <div class="container-fluid mt-3">
        <h1>{% block page_title %}{% block content_title %}{% endblock %}{% endblock %}</h1>
        
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ type }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}
        
        {% block main %}{% endblock %}
    </div>

    {# Add links to Bootstrap JS or other necessary JS if needed #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block body_javascripts %}{% endblock %}
</body>
</html> 